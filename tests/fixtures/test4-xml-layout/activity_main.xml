<?xml version="1.0" encoding="utf-8"?>
<!-- 
  activity_main.xml - XML Layout Error Example
  
  ERROR at line 12: Binary XML file error during inflation
  
  Problem: Unknown attribute "android:textFontWeight" doesn't exist
  - This attribute was added in API 28+ but not available in older Android versions
  - Common mistake: Using attributes that don't exist or require newer API
  
  Runtime error:
    android.view.InflateException: Binary XML file line #12: 
    Error inflating class android.widget.TextView
    
  Cause: Unknown attribute android:textFontWeight
-->
<androidx.constraintlayout.widget.ConstraintLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- Line 12: ERROR - Unknown attribute android:textFontWeight -->
    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to My App"
        android:textSize="24sp"
        android:textFontWeight="700"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp" />

    <!-- This works fine -->
    <Button
        android:id="@+id/loginButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login"
        app:layout_constraintTop_toBottomOf="@id/titleText"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp" />

</androidx.constraintlayout.widget.ConstraintLayout>

<!--
  FIX OPTIONS:
  
  Option 1: Remove the invalid attribute
    <TextView
        ...
        android:textSize="24sp"
        (remove android:textFontWeight)
        android:textStyle="bold"  <- Use this instead
        ... />
  
  Option 2: Use android:textStyle for basic font weights
    android:textStyle="bold"
  
  Option 3: Use custom font with FontFamily (for precise weights)
    android:fontFamily="@font/roboto_bold"
  
  Option 4: Use TextAppearance
    android:textAppearance="@style/TextAppearance.AppCompat.Headline"
-->
